## 锐捷VLAN配置

- 在锐捷交换机上配置VLAN可以实现网络的逻辑分段，提高网络管理的灵活性和安全性。以下是一个基本的VLAN配置示例。

- 创建VLAN

```bash
S2724G#conf
S2724G(config)#vlan 10
S2724G(config-vlan)#exit
S2724G(config)#vlan 20
S2724G(config-vlan)#exit
```

- 分配接口到VLAN

```bash
S2724G(config)#int gi 0/10
S2724G(config-if)#switchport mode access
S2724G(config-if)#switchport access vlan 10
S2724G(config-if)#exit

S2724G(config)#int gi 0/20
S2724G(config-if)#switchport mode access
S2724G(config-if)#switchport access vlan 20
S2724G(config-if)#exit
```

- 配置Trunk接口
> Trunk接口 是一种允许多个VLAN通过的交换机端口类型，通常用于连接交换机之间的端口。Trunk接口可以接收和发送多个VLAN的报文，通过在数据帧中添加VLAN标签来区分不同的VLAN数据。
> - Trunk接口的作用
> 1. 跨交换机的VLAN通信：Trunk接口允许连接在不同交换机上的相同VLAN中的主机互通。例如，如果交换机1的VLAN1中的机器要访问交换机2的VLAN1中的机器，可以将两台交换机的直连端口设置为Trunk端口。
> 2. 承载多个VLAN：Trunk接口可以承载多个VLAN的数据，只需用一条中继链路承载所有的VLAN通信。

```bash
S2724G(config)#int gi 0/24
S2724G(config-if)#switchport mode trunk
S2724G(config-if)#exit
```
- 查看VLAN信息
```bash
S2724G#show vlan brief
```
- 删除VLAN

```bash
S2724G#conf
S2724G(config)#no vlan 10
S2724G(config)#no vlan 20
```
通过上述步骤，你可以在锐捷交换机上创建和管理VLAN。这些配置命令可以帮助你实现网络的逻辑分段和管理，提高网络的安全性和效率。


- web配置VLAN 参考网址：https://www.ruijie.com.cn/fw/zxpx/71793/


## 开启web管理功能

- 步骤一：用console线登录交换机

- 步骤二：开启交换机的Web服务

```bash
Ruijie>enable           #进入特权模式

Ruijie#configure terminal    #进入配置模式

Ruijie(config)#enable service web-server    #开启客户机的WEB服务
```

- 步骤三：配置管理IP地址

```bash
Ruijie(config)#interface vlan 1   #进入vlan 1接口

Ruijie(config-if)#ip address 192.168.1.1 255.255.255.0    #设置管理ip

Ruijie(config-if)#exit     #退回到全局配置模式
```